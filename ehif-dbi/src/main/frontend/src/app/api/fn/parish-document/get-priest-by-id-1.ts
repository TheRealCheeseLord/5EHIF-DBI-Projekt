/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PriestEmbeddedDto } from '../../models/priest-embedded-dto';

export interface GetPriestById1$Params {
  parishDocumentId: string;
  priestEmbeddedId: string;
}

export function getPriestById1(http: HttpClient, rootUrl: string, params: GetPriestById1$Params, context?: HttpContext): Observable<StrictHttpResponse<PriestEmbeddedDto>> {
  const rb = new RequestBuilder(rootUrl, getPriestById1.PATH, 'get');
  if (params) {
    rb.path('parishDocumentId', params.parishDocumentId, {});
    rb.path('priestEmbeddedId', params.priestEmbeddedId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PriestEmbeddedDto>;
    })
  );
}

getPriestById1.PATH = '/api/parishDocument/{parishDocumentId}/priests/{priestEmbeddedId}';
