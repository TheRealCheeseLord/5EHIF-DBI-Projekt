/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AggregationTestOutputDto } from '../../models/aggregation-test-output-dto';

export interface RunAggregation$Params {
}

export function runAggregation(http: HttpClient, rootUrl: string, params?: RunAggregation$Params, context?: HttpContext): Observable<StrictHttpResponse<AggregationTestOutputDto>> {
  const rb = new RequestBuilder(rootUrl, runAggregation.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AggregationTestOutputDto>;
    })
  );
}

runAggregation.PATH = '/api/benchmarks/aggregation';
