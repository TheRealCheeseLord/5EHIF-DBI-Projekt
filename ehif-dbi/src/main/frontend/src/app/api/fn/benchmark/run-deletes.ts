/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DeleteTestOutputDto } from '../../models/delete-test-output-dto';

export interface RunDeletes$Params {
}

export function runDeletes(http: HttpClient, rootUrl: string, params?: RunDeletes$Params, context?: HttpContext): Observable<StrictHttpResponse<DeleteTestOutputDto>> {
  const rb = new RequestBuilder(rootUrl, runDeletes.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DeleteTestOutputDto>;
    })
  );
}

runDeletes.PATH = '/api/benchmarks/deletes';
