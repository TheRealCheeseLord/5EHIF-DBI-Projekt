/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ParishionerSummaryDto } from '../../models/parishioner-summary-dto';

export interface GetParishioners1$Params {
  parishId: number;
}

export function getParishioners1(http: HttpClient, rootUrl: string, params: GetParishioners1$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<ParishionerSummaryDto>>> {
  const rb = new RequestBuilder(rootUrl, getParishioners1.PATH, 'get');
  if (params) {
    rb.path('parishId', params.parishId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<ParishionerSummaryDto>>;
    })
  );
}

getParishioners1.PATH = '/api/parish/{parishId}/parishioners';
