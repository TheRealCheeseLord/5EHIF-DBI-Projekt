/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ParishionerEmbeddedDto } from '../../models/parishioner-embedded-dto';

export interface GetParishionerById1$Params {
  parishDocumentId: string;
  parishionerEmbeddedId: string;
}

export function getParishionerById1(http: HttpClient, rootUrl: string, params: GetParishionerById1$Params, context?: HttpContext): Observable<StrictHttpResponse<ParishionerEmbeddedDto>> {
  const rb = new RequestBuilder(rootUrl, getParishionerById1.PATH, 'get');
  if (params) {
    rb.path('parishDocumentId', params.parishDocumentId, {});
    rb.path('parishionerEmbeddedId', params.parishionerEmbeddedId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ParishionerEmbeddedDto>;
    })
  );
}

getParishionerById1.PATH = '/api/parishDocument/{parishDocumentId}/parishioners/{parishionerEmbeddedId}';
