/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ReadTestOutputDto } from '../../models/read-test-output-dto';

export interface RunReads$Params {
}

export function runReads(http: HttpClient, rootUrl: string, params?: RunReads$Params, context?: HttpContext): Observable<StrictHttpResponse<ReadTestOutputDto>> {
  const rb = new RequestBuilder(rootUrl, runReads.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ReadTestOutputDto>;
    })
  );
}

runReads.PATH = '/api/benchmarks/reads';
